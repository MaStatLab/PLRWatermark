# Legacy benchmark report

This hand-written report has been retired because it drifted from the generated
artifacts as the experiment gained methods and validation checks. It is kept as
a pointer so old links fail safely rather than presenting stale numerical
claims.

Use these authoritative files instead:

- `benchmark_results.csv` for all clean-benchmark rows;
- `benchmark_summary.json` for configuration, formulas, runtime, and validation
  metadata;
- `paired_comparisons.json` for Wilson intervals, McNemar tests, and complete
  Holm families;
- `regime_sweep.json`, `tail_regime_sweep.json`, and
  `contamination_summary.json` for the robustness experiments;
- `provenance.json` for runtime versions and audited input hashes; and
- `output/pdf/bayesian_pivot_watermark_gumbel.pdf` for the current interpretation.

The repository `README.md` gives the complete regeneration order. This file is
not generated and contains no benchmark result.
